[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so deep links / refreshes resolve to the app (vue-router history mode)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
